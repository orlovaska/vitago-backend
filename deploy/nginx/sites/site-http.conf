# The vitagoguides.ru website before its certificate is issued.
server {
    listen 80;
    server_name ${DOMAIN} www.${DOMAIN};

    include /etc/nginx/snippets/acme.conf;
    include /etc/nginx/snippets/site.conf;
}
