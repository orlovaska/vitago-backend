# API over plain HTTP: before the certificate is issued, and in CI.
server {
    listen 80 default_server;
    server_name ${API_DOMAIN};

    include /etc/nginx/snippets/acme.conf;
    include /etc/nginx/snippets/api.conf;
}
