# Copy to secrets/nginx.env on the server.
# Empty DOMAIN serves only the API on any host name (local runs, CI smoke).
DOMAIN=vitagoguides.ru
# Defaults to api.<DOMAIN>.
API_DOMAIN=api.vitagoguides.ru
